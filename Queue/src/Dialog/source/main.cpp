#include "Descriptor/Descriptor.h"
// #include "Utility/Utility.h"
#include <iostream>
#include <string>

int main(){

    // size_t N = getValue<int>(std::cin, std::cout, [=](int a){ return a > 0});
    std::string s;
    std::cin>>s;
    std::cout << s;
    return 0;
}
